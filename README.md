# ReadHelper - 文档阅读助手

一个功能强大的 Mac 桌面应用程序，专为文档阅读、翻译、笔记和标记而设计。

## ✨ 功能特性

- 📄 **多格式支持**: 支持 PDF、TXT、Markdown、HTML 等多种文档格式
- 🌐 **智能翻译**: 选中文本即可快速翻译（支持中英互译）
- 📝 **便捷笔记**: 为选中的文本添加笔记和注释
- 🎨 **文本高亮**: 智能高亮显示重要内容
- 🔖 **书签管理**: 快速标记和跳转到重要位置
- 💾 **自动保存**: 笔记和书签自动保存，下次打开时恢复
- 🎯 **现代化界面**: 简洁优雅的 macOS 风格界面

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 运行应用

```bash
npm start
```

### 开发模式

```bash
npm run dev
```

### 打包应用

```bash
npm run build
```

打包后的应用将生成在 `dist` 目录中。

## 📖 使用指南

### 打开文档

1. 点击左侧边栏的「打开文档」按钮
2. 或使用菜单栏：文件 → 打开文档 (⌘+O)
3. 选择您想要阅读的文档文件

### 添加笔记

1. 在文档中选中要添加笔记的文本
2. 点击工具栏的「添加笔记」按钮
3. 在弹出的面板中输入笔记内容
4. 点击「保存」

### 使用翻译

1. 选中需要翻译的文本
2. 点击工具栏的「翻译」按钮
3. 在右下角的翻译面板中查看译文

> **注意**: 当前使用的是模拟翻译。要获得真实的翻译结果，请在 `renderer.js` 中配置翻译 API（如 Google Translate API 或 DeepL API）。

### 添加书签

1. 导航到想要标记的位置
2. 可选：选中一些文本作为书签名称
3. 点击工具栏的「添加书签」按钮
4. 书签将出现在左侧边栏，点击可快速跳转

## 🔧 技术栈

- **Electron**: 跨平台桌面应用框架
- **Node.js**: 运行环境
- **Marked**: Markdown 解析
- **PDF-parse**: PDF 文件解析（预留）

## 🎨 界面预览

应用包含以下主要区域：

- **左侧边栏**: 文件操作、笔记列表、书签列表
- **顶部工具栏**: 文件信息和快捷操作按钮
- **主内容区**: 文档阅读区域
- **右下角面板**: 翻译结果和笔记编辑

## 📝 配置翻译 API

要使用真实的翻译功能，请在 `renderer.js` 中修改 `translateText` 函数：

```javascript
async function translateText(text) {
  // 使用 Google Translate API 示例
  const response = await fetch(
    "https://translation.googleapis.com/language/translate/v2",
    {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        q: text,
        target: "zh-CN", // 目标语言
        key: "YOUR_API_KEY",
      }),
    }
  );

  const data = await response.json();
  return data.data.translations[0].translatedText;
}
```

### 推荐的翻译服务

- [Google Cloud Translation API](https://cloud.google.com/translate)
- [DeepL API](https://www.deepl.com/pro-api)
- [Microsoft Translator](https://azure.microsoft.com/services/cognitive-services/translator/)

## 🛠️ 自定义和扩展

### 添加新的文档格式支持

在 `main.js` 中的 `openDocument` 函数里添加新的文件扩展名：

```javascript
filters: [
  { name: "所有支持的文件", extensions: ["pdf", "txt", "md", "html", "docx"] },
  // 添加更多格式...
];
```

然后在 `renderer.js` 的 `renderDocument` 函数中添加相应的渲染逻辑。

### 自定义主题

修改 `styles.css` 中的颜色变量即可自定义主题：

```css
/* 主色调 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 背景色 */
background-color: #f5f5f7;

/* 文本色 */
color: #1d1d1f;
```

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交问题和拉取请求！

## 📮 联系方式

如有问题或建议，请通过 GitHub Issues 联系。

---

**享受阅读！** 📚✨
